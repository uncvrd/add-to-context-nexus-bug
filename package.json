{
  "name": "add-to-context-bug",
  "license": "UNLICENSED",
  "version": "0.0.0",
  "dependencies": {
    "nexus": "^0.25.0",
    "nexus-plugin-prisma": "^0.16.1"
  },
  "scripts": {
    "format": "npx prettier --write './**/*.{ts,md}'",
    "dev": "nexus dev",
    "build": "nexus build",
    "start": "node .nexus/build/api"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "all"
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  }
}
